# Nested delegation and model routing
`/home/sctru/.no-mistakes/evidence/01M1ZM8FAW3NG1DE8X68AQ608K/nested-routing-demo/effective-config/crew-dispatch.json` is the active firstmate home's current worker-routing authority when that file is present.
Before classification, if that file is present but unreadable, malformed JSON, or invalid under `/home/sctru/.no-mistakes/worktrees/912d26a2b0be/01M1ZM8FAW3NG1DE8X68AQ608K/docs/configuration.md` under Crew dispatch profiles, do not use the static fallback or create a child; report the invalid policy to Firstmate.
Before creating a child through a native subagent or delegation facility, classify the child task under that current policy and explicitly apply the complete selected profile: its configured harness, model, and effort, with an omitted model or effort fixed to the selected harness's default rather than inherited from your session.
If the matching rule or default is a profile array, do not choose among its candidates; report the array to Firstmate, which alone loads `/home/sctru/.no-mistakes/worktrees/912d26a2b0be/01M1ZM8FAW3NG1DE8X68AQ608K/.agents/skills/quota-array-dispatch/SKILL.md` and returns the concrete selection.
If no rule matches and the policy has no default, read `/home/sctru/.no-mistakes/evidence/01M1ZM8FAW3NG1DE8X68AQ608K/nested-routing-demo/effective-config/crew-harness` at child intake.
A current concrete adapter in that file may be used only if `/home/sctru/.no-mistakes/worktrees/912d26a2b0be/01M1ZM8FAW3NG1DE8X68AQ608K/AGENTS.md` section 4 verifies it for crewmates or scouts, selecting that harness with its default model and effort.
If the file is absent, empty, or `default`, use the captured Firstmate baseline `claude` with that harness's default model and effort.
If the current value is unverified, report that exact value to Firstmate and use the captured baseline instead.
Use the captured baseline only if `/home/sctru/.no-mistakes/worktrees/912d26a2b0be/01M1ZM8FAW3NG1DE8X68AQ608K/AGENTS.md` section 4 verifies it for a primary session; otherwise do not create a child and report the unverified baseline to Firstmate.
Include this entire Nested delegation and model routing section unchanged in every native child's instructions so the requirement repeats at every delegation depth.
Create the native child only when its facility can represent that complete selection and receive this section.
If the facility cannot represent every selected axis or deliver this section, do not create the child; report the mismatch to Firstmate so the child can be routed through the fleet lifecycle.
Do not use legacy Fable/Opus/Sonnet/Haiku routing tiers to select a child profile.
A model explicitly selected by `/home/sctru/.no-mistakes/evidence/01M1ZM8FAW3NG1DE8X68AQ608K/nested-routing-demo/effective-config/crew-dispatch.json` remains authoritative even when its name contains one of those words.
Firstmate cannot intercept every third-party native child facility, so this is a briefing requirement rather than claimed runtime enforcement for those tools.

